---
title: CA1062：验证公共方法的参数（代码分析）
description: 了解代码分析规则 CA1062：验证公共方法的参数
ms.date: 12/18/2020
ms.topic: reference
f1_keywords:
- CA1062
- ValidateArgumentsOfPublicMethods
- Validate arguments of public methods
helpviewer_keywords:
- CA1062
- ValidateArgumentsOfPublicMethods
author: gewarren
ms.author: gewarren
dev_langs:
- CSharp
- VB
ms.openlocfilehash: 80d1e7e1f605617196725a49f157ffe952342111
ms.sourcegitcommit: 05d0087dfca85aac9ca2960f86c5efd218bf833f
ms.translationtype: HT
ms.contentlocale: zh-CN
ms.lasthandoff: 03/30/2021
ms.locfileid: "99546720"
---
# <a name="ca1062-validate-arguments-of-public-methods"></a><span data-ttu-id="296aa-103">CA1062:验证公共方法的参数</span><span class="sxs-lookup"><span data-stu-id="296aa-103">CA1062: Validate arguments of public methods</span></span>

| | <span data-ttu-id="296aa-104">值</span><span class="sxs-lookup"><span data-stu-id="296aa-104">Value</span></span> |
|-|-|
| <span data-ttu-id="296aa-105">**规则 ID**</span><span class="sxs-lookup"><span data-stu-id="296aa-105">**Rule ID**</span></span> |<span data-ttu-id="296aa-106">CA1062</span><span class="sxs-lookup"><span data-stu-id="296aa-106">CA1062</span></span>|
| <span data-ttu-id="296aa-107">**类别**</span><span class="sxs-lookup"><span data-stu-id="296aa-107">**Category**</span></span> |[<span data-ttu-id="296aa-108">Microsoft.Design</span><span class="sxs-lookup"><span data-stu-id="296aa-108">Microsoft.Design</span></span>](design-warnings.md)|
| <span data-ttu-id="296aa-109">修复是中断修复还是非中断修复</span><span class="sxs-lookup"><span data-stu-id="296aa-109">**Fix is breaking or non-breaking**</span></span> |<span data-ttu-id="296aa-110">非中断</span><span class="sxs-lookup"><span data-stu-id="296aa-110">Non-breaking</span></span>|

## <a name="cause"></a><span data-ttu-id="296aa-111">原因</span><span class="sxs-lookup"><span data-stu-id="296aa-111">Cause</span></span>

<span data-ttu-id="296aa-112">外部可见方法取消引用其中一个引用参数，而不验证该参数是否 `null`（Visual Basic 中 `Nothing`）。</span><span class="sxs-lookup"><span data-stu-id="296aa-112">An externally visible method dereferences one of its reference arguments without verifying whether that argument is `null` (`Nothing` in Visual Basic).</span></span>

<span data-ttu-id="296aa-113">可以将此规则[配置](#configure-code-to-analyze)为从分析中排除某些类型和参数。</span><span class="sxs-lookup"><span data-stu-id="296aa-113">You can [configure](#configure-code-to-analyze) this rule to exclude certain types and parameters from analysis.</span></span> <span data-ttu-id="296aa-114">还可以[指示 null 检查验证方法](#null-check-validation-methods)。</span><span class="sxs-lookup"><span data-stu-id="296aa-114">You can also [indicate null-check validation methods](#null-check-validation-methods).</span></span>

## <a name="rule-description"></a><span data-ttu-id="296aa-115">规则说明</span><span class="sxs-lookup"><span data-stu-id="296aa-115">Rule description</span></span>

<span data-ttu-id="296aa-116">对于传递给外部可见方法的所有引用参数，都应检查其是否为 `null`。</span><span class="sxs-lookup"><span data-stu-id="296aa-116">All reference arguments that are passed to externally visible methods should be checked against `null`.</span></span> <span data-ttu-id="296aa-117">如果需要，则在参数为 `null` 时引发 <xref:System.ArgumentNullException>。</span><span class="sxs-lookup"><span data-stu-id="296aa-117">If appropriate, throw a <xref:System.ArgumentNullException> when the argument is `null`.</span></span>

<span data-ttu-id="296aa-118">如果某个方法由于被声明为公共或受保护而可以从未知程序集进行调用，则应验证该方法的所有参数。</span><span class="sxs-lookup"><span data-stu-id="296aa-118">If a method can be called from an unknown assembly because it is declared public or protected, you should validate all parameters of the method.</span></span> <span data-ttu-id="296aa-119">如果该方法设计为仅由已知程序集调用，请将方法标记为 `internal`并将 <xref:System.Runtime.CompilerServices.InternalsVisibleToAttribute> 特性应用于包含该方法的程序集。</span><span class="sxs-lookup"><span data-stu-id="296aa-119">If the method is designed to be called only by known assemblies, mark the method `internal` and apply the <xref:System.Runtime.CompilerServices.InternalsVisibleToAttribute> attribute to the assembly that contains the method.</span></span>

## <a name="how-to-fix-violations"></a><span data-ttu-id="296aa-120">如何解决冲突</span><span class="sxs-lookup"><span data-stu-id="296aa-120">How to fix violations</span></span>

<span data-ttu-id="296aa-121">若要修复与此规则的冲突，请验证每个引用参数是否为 `null`。</span><span class="sxs-lookup"><span data-stu-id="296aa-121">To fix a violation of this rule, validate each reference argument against `null`.</span></span>

## <a name="when-to-suppress-warnings"></a><span data-ttu-id="296aa-122">何时禁止显示警告</span><span class="sxs-lookup"><span data-stu-id="296aa-122">When to suppress warnings</span></span>

<span data-ttu-id="296aa-123">如果确定取消引用的参数已由函数中的其他方法调用进行验证，则可以禁止显示此规则发出的警告。</span><span class="sxs-lookup"><span data-stu-id="296aa-123">You can suppress a warning from this rule if you are sure that the dereferenced parameter has been validated by another method call in the function.</span></span>

## <a name="configure-code-to-analyze"></a><span data-ttu-id="296aa-124">配置代码以进行分析</span><span class="sxs-lookup"><span data-stu-id="296aa-124">Configure code to analyze</span></span>

<span data-ttu-id="296aa-125">使用下面的选项来配置代码库的哪些部分要运行此规则。</span><span class="sxs-lookup"><span data-stu-id="296aa-125">Use the following options to configure which parts of your codebase to run this rule on.</span></span>

- [<span data-ttu-id="296aa-126">排除特定符号</span><span class="sxs-lookup"><span data-stu-id="296aa-126">Exclude specific symbols</span></span>](#exclude-specific-symbols)
- [<span data-ttu-id="296aa-127">排除特定类型及其派生类型</span><span class="sxs-lookup"><span data-stu-id="296aa-127">Exclude specific types and their derived types</span></span>](#exclude-specific-types-and-their-derived-types)
- [<span data-ttu-id="296aa-128">排除扩展方法“this”参数</span><span class="sxs-lookup"><span data-stu-id="296aa-128">Exclude extension method 'this' parameter</span></span>](#exclude-extension-method-this-parameter)
- [<span data-ttu-id="296aa-129">Null 检查验证方法</span><span class="sxs-lookup"><span data-stu-id="296aa-129">Null check validation methods</span></span>](#null-check-validation-methods)

<span data-ttu-id="296aa-130">可以仅为此规则、为所有规则或为此类别（[设计](design-warnings.md)）中的所有规则配置这些选项。</span><span class="sxs-lookup"><span data-stu-id="296aa-130">These options can be configured for just this rule, for all rules, or for all rules in this category ([Design](design-warnings.md)).</span></span> <span data-ttu-id="296aa-131">有关详细信息，请参阅[代码质量规则配置选项](../code-quality-rule-options.md)。</span><span class="sxs-lookup"><span data-stu-id="296aa-131">For more information, see [Code quality rule configuration options](../code-quality-rule-options.md).</span></span>

[!INCLUDE[excluded-symbol-names](~/includes/code-analysis/excluded-symbol-names.md)]

[!INCLUDE[excluded-type-names-with-derived-types](~/includes/code-analysis/excluded-type-names-with-derived-types.md)]

### <a name="exclude-extension-method-this-parameter"></a><span data-ttu-id="296aa-132">排除扩展方法“this”参数</span><span class="sxs-lookup"><span data-stu-id="296aa-132">Exclude extension method 'this' parameter</span></span>

<span data-ttu-id="296aa-133">默认情况下，此规则分析并标记扩展方法的 `this` 参数。</span><span class="sxs-lookup"><span data-stu-id="296aa-133">By default, this rule analyzes and flags the `this` parameter for extension methods.</span></span> <span data-ttu-id="296aa-134">可以通过将以下键值对添加到项目中的 editorconfig 文件，排除扩展方法的 `this` 参数的分析：</span><span class="sxs-lookup"><span data-stu-id="296aa-134">You can exclude analysis of the `this` parameter for extension methods by adding the following key-value pair to an *.editorconfig* file in your project:</span></span>

```ini
dotnet_code_quality.CA1062.exclude_extension_method_this_parameter = true
```

### <a name="null-check-validation-methods"></a><span data-ttu-id="296aa-135">Null 检查验证方法</span><span class="sxs-lookup"><span data-stu-id="296aa-135">Null check validation methods</span></span>

<span data-ttu-id="296aa-136">如果代码在引用的库或项目中调用了特殊的 null 检查验证方法，则此规则可能导致误报。</span><span class="sxs-lookup"><span data-stu-id="296aa-136">This rule can lead to false positives if your code calls special null-check validation methods in referenced libraries or projects.</span></span> <span data-ttu-id="296aa-137">可以通过指定 null 检查验证方法的名称或签名来避免这种误报。</span><span class="sxs-lookup"><span data-stu-id="296aa-137">You can avoid these false positives by specifying the name or signature of null-check validation methods.</span></span> <span data-ttu-id="296aa-138">此分析假定在调用后传递给这些方法的参数为非 null。</span><span class="sxs-lookup"><span data-stu-id="296aa-138">The analysis assumes that arguments passed to these methods are non-null after the call.</span></span> <span data-ttu-id="296aa-139">例如，若要将名为 `Validate` 的所有方法标记为 null 检查验证方法，请将以下键值对添加到项目中的 editorconfig 文件：</span><span class="sxs-lookup"><span data-stu-id="296aa-139">For example, to mark all methods named `Validate` as null-check validation methods, add the following key-value pair to an *.editorconfig* file in your project:</span></span>

```ini
dotnet_code_quality.CA1062.null_check_validation_methods = Validate
```

<span data-ttu-id="296aa-140">选项值中允许的方法名称格式（用 `|` 分隔）：</span><span class="sxs-lookup"><span data-stu-id="296aa-140">Allowed method name formats in the option value (separated by `|`):</span></span>

- <span data-ttu-id="296aa-141">仅方法名称（包括具有相应名称的所有方法，不考虑包含的类型或命名空间）。</span><span class="sxs-lookup"><span data-stu-id="296aa-141">Method name only (includes all methods with the name, regardless of the containing type or namespace).</span></span>
- <span data-ttu-id="296aa-142">完全限定的名称，使用符号的[文档 ID 格式](../../../csharp/programming-guide/xmldoc/processing-the-xml-file.md#id-strings)，前缀为 `M:`（可选）。</span><span class="sxs-lookup"><span data-stu-id="296aa-142">Fully qualified names in the symbol's [documentation ID format](../../../csharp/programming-guide/xmldoc/processing-the-xml-file.md#id-strings), with an optional `M:` prefix.</span></span>

<span data-ttu-id="296aa-143">示例：</span><span class="sxs-lookup"><span data-stu-id="296aa-143">Examples:</span></span>

| <span data-ttu-id="296aa-144">选项值</span><span class="sxs-lookup"><span data-stu-id="296aa-144">Option Value</span></span> | <span data-ttu-id="296aa-145">总结</span><span class="sxs-lookup"><span data-stu-id="296aa-145">Summary</span></span> |
| --- | --- |
|`dotnet_code_quality.CA1062.null_check_validation_methods = Validate` | <span data-ttu-id="296aa-146">匹配编译中所有名为 `Validate` 的方法</span><span class="sxs-lookup"><span data-stu-id="296aa-146">Matches all methods named `Validate` in the compilation</span></span>
|`dotnet_code_quality.CA1062.null_check_validation_methods = Validate1|Validate2` | <span data-ttu-id="296aa-147">匹配编译中所有名为 `Validate1` 或 `Validate2` 的方法</span><span class="sxs-lookup"><span data-stu-id="296aa-147">Matches all methods named either `Validate1` or `Validate2` in the compilation</span></span>
|`dotnet_code_quality.CA1062.null_check_validation_methods = NS.MyType.Validate(ParamType)` | <span data-ttu-id="296aa-148">将特定方法 `Validate` 与给定的完全限定签名相匹配</span><span class="sxs-lookup"><span data-stu-id="296aa-148">Matches specific method `Validate` with given fully qualified signature</span></span>
|`dotnet_code_quality.CA1062.null_check_validation_methods = NS1.MyType1.Validate1(ParamType)|NS2.MyType2.Validate2(ParamType)` | <span data-ttu-id="296aa-149">将特定方法 `Validate1` 和 `Validate2` 与相应的完全限定签名相匹配</span><span class="sxs-lookup"><span data-stu-id="296aa-149">Matches specific methods `Validate1` and `Validate2` with respective fully qualified signature</span></span>

## <a name="example-1"></a><span data-ttu-id="296aa-150">示例 1</span><span class="sxs-lookup"><span data-stu-id="296aa-150">Example 1</span></span>

<span data-ttu-id="296aa-151">下面的示例演示了违反规则的方法和符合规则的方法。</span><span class="sxs-lookup"><span data-stu-id="296aa-151">The following example shows a method that violates the rule and a method that satisfies the rule.</span></span>

```csharp
using System;

namespace DesignLibrary
{
    public class Test
    {
        // This method violates the rule.
        public void DoNotValidate(string input)
        {
            if (input.Length != 0)
            {
                Console.WriteLine(input);
            }
        }

        // This method satisfies the rule.
        public void Validate(string input)
        {
            if (input == null)
            {
                throw new ArgumentNullException(nameof(input));
            }
            if (input.Length != 0)
            {
                Console.WriteLine(input);
            }
        }
    }
}
```

```vb
Imports System

Namespace DesignLibrary

    Public Class Test

        ' This method violates the rule.
        Sub DoNotValidate(ByVal input As String)

            If input.Length <> 0 Then
                Console.WriteLine(input)
            End If

        End Sub

        ' This method satisfies the rule.
        Sub Validate(ByVal input As String)

            If input Is Nothing Then
                Throw New ArgumentNullException(NameOf(input))
            End If

            If input.Length <> 0 Then
                Console.WriteLine(input)
            End If

        End Sub

    End Class

End Namespace
```

## <a name="example-2"></a><span data-ttu-id="296aa-152">示例 2</span><span class="sxs-lookup"><span data-stu-id="296aa-152">Example 2</span></span>

<span data-ttu-id="296aa-153">填充为引用对象的字段或属性的复制构造函数也可能与 CA1062 规则发生冲突。</span><span class="sxs-lookup"><span data-stu-id="296aa-153">Copy constructors that populate field or properties that are reference objects can also violate rule CA1062.</span></span> <span data-ttu-id="296aa-154">发生冲突的原因是，传递到复制构造函数的所复制对象可能为 `null`（在 Visual Basic 中为 `Nothing`）。</span><span class="sxs-lookup"><span data-stu-id="296aa-154">The violation occurs because the copied object that's passed to the copy constructor might be `null` (`Nothing` in Visual Basic).</span></span> <span data-ttu-id="296aa-155">若要解决冲突，请使用 `static`（在 Visual Basic 中为 `Shared`）方法来检查复制的对象是否不为 null。</span><span class="sxs-lookup"><span data-stu-id="296aa-155">To resolve the violation, use a `static` (`Shared` in Visual Basic) method to check that the copied object is not null.</span></span>

<span data-ttu-id="296aa-156">在下面的 `Person` 类示例中，传递给 `Person` 复制构造函数的 `other` 对象可能为 `null`。</span><span class="sxs-lookup"><span data-stu-id="296aa-156">In the following `Person` class example, the `other` object that is passed to the `Person` copy constructor might be `null`.</span></span>

```csharp
public class Person
{
    public string Name { get; private set; }
    public int Age { get; private set; }

    public Person(string name, int age)
    {
        Name = name;
        Age = age;
    }

    // Copy constructor CA1062 fires because other is dereferenced
    // without being checked for null
    public Person(Person other)
        : this(other.Name, other.Age)
    {
    }
}
```

## <a name="example-3"></a><span data-ttu-id="296aa-157">示例 3</span><span class="sxs-lookup"><span data-stu-id="296aa-157">Example 3</span></span>

<span data-ttu-id="296aa-158">在下面经修订的 `Person` 示例中，系统首先会在 `PassThroughNonNull` 方法中检查传递给复制构造函数的 `other` 对象是否为 null。</span><span class="sxs-lookup"><span data-stu-id="296aa-158">In the following revised `Person` example, the `other` object that's passed to the copy constructor is first checked for null in the `PassThroughNonNull` method.</span></span>

```csharp
public class Person
{
    public string Name { get; private set; }
    public int Age { get; private set; }

    public Person(string name, int age)
    {
        Name = name;
        Age = age;
    }

    // Copy constructor
    public Person(Person other)
        : this(PassThroughNonNull(other).Name, other.Age)
    {
    }

    // Null check method
    private static Person PassThroughNonNull(Person person)
    {
        if (person == null)
            throw new ArgumentNullException(nameof(person));
        return person;
    }
}
```
