---
title: CA2000：超出范围前释放对象（代码分析）
description: 了解代码分析规则 CA2000：超出范围前释放对象
ms.date: 05/14/2019
ms.topic: reference
f1_keywords:
- CA2000
- Dispose objects before losing scope
- DisposeObjectsBeforeLosingScope
helpviewer_keywords:
- CA2000
- DisposeObjectsBeforeLosingScope
author: gewarren
ms.author: gewarren
dev_langs:
- CSharp
- VB
ms.openlocfilehash: 2e8f2d58ae8f8799fde51cc15522277cbc90988a
ms.sourcegitcommit: 05d0087dfca85aac9ca2960f86c5efd218bf833f
ms.translationtype: HT
ms.contentlocale: zh-CN
ms.lasthandoff: 03/30/2021
ms.locfileid: "99792422"
---
# <a name="ca2000-dispose-objects-before-losing-scope"></a><span data-ttu-id="d05bd-103">CA2000:丢失范围之前释放对象</span><span class="sxs-lookup"><span data-stu-id="d05bd-103">CA2000: Dispose objects before losing scope</span></span>

| | <span data-ttu-id="d05bd-104">值</span><span class="sxs-lookup"><span data-stu-id="d05bd-104">Value</span></span> |
|-|-|
| <span data-ttu-id="d05bd-105">**规则 ID**</span><span class="sxs-lookup"><span data-stu-id="d05bd-105">**Rule ID**</span></span> |<span data-ttu-id="d05bd-106">CA2000</span><span class="sxs-lookup"><span data-stu-id="d05bd-106">CA2000</span></span>|
| <span data-ttu-id="d05bd-107">**类别**</span><span class="sxs-lookup"><span data-stu-id="d05bd-107">**Category**</span></span> |[<span data-ttu-id="d05bd-108">可靠性</span><span class="sxs-lookup"><span data-stu-id="d05bd-108">Reliability</span></span>](reliability-warnings.md)|
| <span data-ttu-id="d05bd-109">修复是中断修复还是非中断修复</span><span class="sxs-lookup"><span data-stu-id="d05bd-109">**Fix is breaking or non-breaking**</span></span> |<span data-ttu-id="d05bd-110">非中断</span><span class="sxs-lookup"><span data-stu-id="d05bd-110">Non-breaking</span></span>|

## <a name="cause"></a><span data-ttu-id="d05bd-111">原因</span><span class="sxs-lookup"><span data-stu-id="d05bd-111">Cause</span></span>

<span data-ttu-id="d05bd-112">创建了 <xref:System.IDisposable> 类型的本地对象，但该对象不会被释放，除非对对象的所有引用都超出范围。</span><span class="sxs-lookup"><span data-stu-id="d05bd-112">A local object of an <xref:System.IDisposable> type is created, but the object is not disposed before all references to the object are out of scope.</span></span>

<span data-ttu-id="d05bd-113">默认情况下，此规则会分析整个代码库，但这是[可配置](#configure-code-to-analyze)的。</span><span class="sxs-lookup"><span data-stu-id="d05bd-113">By default, this rule analyzes the entire codebase, but this is [configurable](#configure-code-to-analyze).</span></span>

## <a name="rule-description"></a><span data-ttu-id="d05bd-114">规则说明</span><span class="sxs-lookup"><span data-stu-id="d05bd-114">Rule description</span></span>

<span data-ttu-id="d05bd-115">如果在对某个可释放对象的所有引用超出范围之前未显式释放该对象，则当垃圾回收器运行该对象的终结器时，将在某个不确定的时间释放该对象。</span><span class="sxs-lookup"><span data-stu-id="d05bd-115">If a disposable object is not explicitly disposed before all references to it are out of scope, the object will be disposed at some indeterminate time when the garbage collector runs the finalizer of the object.</span></span> <span data-ttu-id="d05bd-116">由于可能发生异常事件，导致对象的终结器无法运行，因此应显式释放对象。</span><span class="sxs-lookup"><span data-stu-id="d05bd-116">Because an exceptional event might occur that will prevent the finalizer of the object from running, the object should be explicitly disposed instead.</span></span>

## <a name="special-cases"></a><span data-ttu-id="d05bd-117">特殊情况</span><span class="sxs-lookup"><span data-stu-id="d05bd-117">Special cases</span></span>

<span data-ttu-id="d05bd-118">即使未释放对象，也不会由于以下类型的本地对象触发规则 CA2000：</span><span class="sxs-lookup"><span data-stu-id="d05bd-118">Rule CA2000 does not fire for local objects of the following types even if the object is not disposed:</span></span>

- <xref:System.IO.Stream?displayProperty=nameWithType>
- <xref:System.IO.StringReader?displayProperty=nameWithType>
- <xref:System.IO.TextReader?displayProperty=nameWithType>
- <xref:System.IO.TextWriter?displayProperty=nameWithType>
- <xref:System.Resources.IResourceReader?displayProperty=nameWithType>

<span data-ttu-id="d05bd-119">将其中一个类型的对象传递给构造函数，然后将其分配给一个字段，表示释放所有权转移到新构造的类型。</span><span class="sxs-lookup"><span data-stu-id="d05bd-119">Passing an object of one of these types to a constructor and then assigning it to a field indicates a *dispose ownership transfer* to the newly constructed type.</span></span> <span data-ttu-id="d05bd-120">也就是说，新构造的类型现在负责释放对象。</span><span class="sxs-lookup"><span data-stu-id="d05bd-120">That is, the newly constructed type is now responsible for disposing of the object.</span></span> <span data-ttu-id="d05bd-121">如果代码将其中一个类型的对象传递给构造函数，则即使在对对象的所有引用超出范围之前未释放该对象，也不会发生规则 CA2000 冲突。</span><span class="sxs-lookup"><span data-stu-id="d05bd-121">If your code passes an object of one of these types to a constructor, no violation of rule CA2000 occurs even if the object is not disposed before all references to it are out of scope.</span></span>

## <a name="how-to-fix-violations"></a><span data-ttu-id="d05bd-122">如何解决冲突</span><span class="sxs-lookup"><span data-stu-id="d05bd-122">How to fix violations</span></span>

<span data-ttu-id="d05bd-123">要解决此规则的冲突，需在对对象的所有引用超出范围之前，在对象上调用 <xref:System.IDisposable.Dispose%2A>。</span><span class="sxs-lookup"><span data-stu-id="d05bd-123">To fix a violation of this rule, call <xref:System.IDisposable.Dispose%2A> on the object before all references to it are out of scope.</span></span>

<span data-ttu-id="d05bd-124">可使用 [`using` 语句](../../../csharp/language-reference/keywords/using-statement.md)（Visual Basic 中的 [`Using`](../../../visual-basic/language-reference/statements/using-statement.md)）来包装实现 <xref:System.IDisposable> 的对象。</span><span class="sxs-lookup"><span data-stu-id="d05bd-124">You can use the [`using` statement](../../../csharp/language-reference/keywords/using-statement.md) ([`Using`](../../../visual-basic/language-reference/statements/using-statement.md) in Visual Basic) to wrap objects that implement <xref:System.IDisposable>.</span></span> <span data-ttu-id="d05bd-125">以这种方式包装的对象将自动在 `using` 块的末尾释放。</span><span class="sxs-lookup"><span data-stu-id="d05bd-125">Objects that are wrapped in this manner are automatically disposed at the end of the `using` block.</span></span> <span data-ttu-id="d05bd-126">但是，以下情况不应或不能使用 `using` 语句进行处理：</span><span class="sxs-lookup"><span data-stu-id="d05bd-126">However, the following situations should not or cannot be handled with a `using` statement:</span></span>

- <span data-ttu-id="d05bd-127">要返回可释放对象，该对象必须在 `using` 块外的 `try/finally` 块中构造。</span><span class="sxs-lookup"><span data-stu-id="d05bd-127">To return a disposable object, the object must constructed in a `try/finally` block outside of a `using` block.</span></span>

- <span data-ttu-id="d05bd-128">请勿在 `using` 语句的构造函数中初始化可释放对象的成员。</span><span class="sxs-lookup"><span data-stu-id="d05bd-128">Do not initialize members of a disposable object in the constructor of a `using` statement.</span></span>

- <span data-ttu-id="d05bd-129">如果构造函数仅由一个异常处理程序保护并嵌套在 [`using` 语句的获取部分](../../../csharp/language-reference/keywords/using-statement.md)，则外部构造函数中的失败会导致始终不会关闭嵌套构造函数所创建的对象。</span><span class="sxs-lookup"><span data-stu-id="d05bd-129">When constructors that are protected by only one exception handler are nested in the [acquisition part of a `using` statement](../../../csharp/language-reference/keywords/using-statement.md), a failure in the outer constructor can result in the object created by the nested constructor never being closed.</span></span> <span data-ttu-id="d05bd-130">在下面的示例中，<xref:System.IO.StreamReader> 构造函数中的失败会导致始终不会关闭 <xref:System.IO.FileStream> 对象。</span><span class="sxs-lookup"><span data-stu-id="d05bd-130">In the following example, a failure in the <xref:System.IO.StreamReader> constructor can result in the <xref:System.IO.FileStream> object never being closed.</span></span> <span data-ttu-id="d05bd-131">在这种情况下，CA2000 标记为违反规则。</span><span class="sxs-lookup"><span data-stu-id="d05bd-131">CA2000 flags a violation of the rule in this case.</span></span>

   ```csharp
   using (StreamReader sr = new StreamReader(new FileStream("C:\myfile.txt", FileMode.Create)))
   { ... }
   ```

- <span data-ttu-id="d05bd-132">动态对象应使用影子对象实现 <xref:System.IDisposable> 对象的释放模式。</span><span class="sxs-lookup"><span data-stu-id="d05bd-132">Dynamic objects should use a shadow object to implement the dispose pattern of <xref:System.IDisposable> objects.</span></span>

## <a name="when-to-suppress-warnings"></a><span data-ttu-id="d05bd-133">何时禁止显示警告</span><span class="sxs-lookup"><span data-stu-id="d05bd-133">When to suppress warnings</span></span>

<span data-ttu-id="d05bd-134">在以下情况可禁止显示此规则发出的警告：</span><span class="sxs-lookup"><span data-stu-id="d05bd-134">Do not suppress a warning from this rule unless:</span></span>

- <span data-ttu-id="d05bd-135">在对象上调用了一个调用 `Dispose` 的方法，例如 <xref:System.IO.Stream.Close%2A></span><span class="sxs-lookup"><span data-stu-id="d05bd-135">You've called a method on your object that calls `Dispose`, such as <xref:System.IO.Stream.Close%2A></span></span>
- <span data-ttu-id="d05bd-136">引发警告的方法返回包装对象的 <xref:System.IDisposable> 对象</span><span class="sxs-lookup"><span data-stu-id="d05bd-136">The method that raised the warning returns an <xref:System.IDisposable> object that wraps your object</span></span>
- <span data-ttu-id="d05bd-137">分配方法没有释放所有权；也就是说，释放对象的责任将转移到在方法中创建并返回给调用方的另一个对象或包装器</span><span class="sxs-lookup"><span data-stu-id="d05bd-137">The allocating method does not have dispose ownership; that is, the responsibility to dispose the object is transferred to another object or wrapper that's created in the method and returned to the caller</span></span>

## <a name="configure-code-to-analyze"></a><span data-ttu-id="d05bd-138">配置代码以进行分析</span><span class="sxs-lookup"><span data-stu-id="d05bd-138">Configure code to analyze</span></span>

<span data-ttu-id="d05bd-139">使用下面的选项来配置代码库的哪些部分要运行此规则。</span><span class="sxs-lookup"><span data-stu-id="d05bd-139">Use the following options to configure which parts of your codebase to run this rule on.</span></span>

- [<span data-ttu-id="d05bd-140">排除特定符号</span><span class="sxs-lookup"><span data-stu-id="d05bd-140">Exclude specific symbols</span></span>](#exclude-specific-symbols)
- [<span data-ttu-id="d05bd-141">排除特定类型及其派生类型</span><span class="sxs-lookup"><span data-stu-id="d05bd-141">Exclude specific types and their derived types</span></span>](#exclude-specific-types-and-their-derived-types)

<span data-ttu-id="d05bd-142">可以仅为此规则、为所有规则或为此类别（[可靠性](reliability-warnings.md)）中的所有规则配置这些选项。</span><span class="sxs-lookup"><span data-stu-id="d05bd-142">You can configure these options for just this rule, for all rules, or for all rules in this category ([Reliability](reliability-warnings.md)).</span></span> <span data-ttu-id="d05bd-143">有关详细信息，请参阅[代码质量规则配置选项](../code-quality-rule-options.md)。</span><span class="sxs-lookup"><span data-stu-id="d05bd-143">For more information, see [Code quality rule configuration options](../code-quality-rule-options.md).</span></span>

[!INCLUDE[excluded-symbol-names](~/includes/code-analysis/excluded-symbol-names.md)]

[!INCLUDE[excluded-type-names-with-derived-types](~/includes/code-analysis/excluded-type-names-with-derived-types.md)]

## <a name="related-rules"></a><span data-ttu-id="d05bd-144">相关规则</span><span class="sxs-lookup"><span data-stu-id="d05bd-144">Related rules</span></span>

- [<span data-ttu-id="d05bd-145">CA2213:应释放可释放的字段</span><span class="sxs-lookup"><span data-stu-id="d05bd-145">CA2213: Disposable fields should be disposed</span></span>](ca2213.md)

## <a name="example-1"></a><span data-ttu-id="d05bd-146">示例 1</span><span class="sxs-lookup"><span data-stu-id="d05bd-146">Example 1</span></span>

<span data-ttu-id="d05bd-147">如果要实现返回可释放对象的方法，可在没有 catch 块的情况下使用 try/finally 块来确保释放对象。</span><span class="sxs-lookup"><span data-stu-id="d05bd-147">If you're implementing a method that returns a disposable object, use a try/finally block without a catch block to make sure that the object is disposed.</span></span> <span data-ttu-id="d05bd-148">通过使用 try/finally 块，允许在故障点引发异常，并确保对象已释放。</span><span class="sxs-lookup"><span data-stu-id="d05bd-148">By using a try/finally block, you allow exceptions to be raised at the fault point and make sure that object is disposed.</span></span>

<span data-ttu-id="d05bd-149">在 OpenPort1 方法中，调用打开 ISerializable 对象 SerialPort 或对 SomeMethod 的调用可能会失败。</span><span class="sxs-lookup"><span data-stu-id="d05bd-149">In the OpenPort1 method, the call to open the ISerializable object SerialPort or the call to SomeMethod can fail.</span></span> <span data-ttu-id="d05bd-150">此实现会引发 CA2000 警告。</span><span class="sxs-lookup"><span data-stu-id="d05bd-150">A CA2000 warning is raised on this implementation.</span></span>

<span data-ttu-id="d05bd-151">在 OpenPort2 方法中，将声明两个 SerialPort 对象并将其设置为 null：</span><span class="sxs-lookup"><span data-stu-id="d05bd-151">In the OpenPort2 method, two SerialPort objects are declared and set to null:</span></span>

- <span data-ttu-id="d05bd-152">`tempPort`，用于测试方法操作是否成功。</span><span class="sxs-lookup"><span data-stu-id="d05bd-152">`tempPort`, which is used to test that the method operations succeed.</span></span>

- <span data-ttu-id="d05bd-153">`port`，用于返回方法的值。</span><span class="sxs-lookup"><span data-stu-id="d05bd-153">`port`, which is used for the return value of the method.</span></span>

<span data-ttu-id="d05bd-154">构造 `tempPort` 并在 `try` 块中打开，以及在相同的 `try` 块中执行任何其他所需的工作。</span><span class="sxs-lookup"><span data-stu-id="d05bd-154">The `tempPort` is constructed and opened in a `try` block, and any other required work is performed in the same `try` block.</span></span> <span data-ttu-id="d05bd-155">在 `try` 块的末尾，打开的端口分配给将返回的 `port` 对象，`tempPort` 对象设置为 `null`。</span><span class="sxs-lookup"><span data-stu-id="d05bd-155">At the end of the `try` block, the opened port is assigned to the `port` object that will be returned and the `tempPort` object is set to `null`.</span></span>

<span data-ttu-id="d05bd-156">`finally` 块检查 `tempPort` 的值。</span><span class="sxs-lookup"><span data-stu-id="d05bd-156">The `finally` block checks the value of `tempPort`.</span></span> <span data-ttu-id="d05bd-157">如果不为 null，则表示方法中的操作失败，`tempPort` 关闭以确保释放所有资源。</span><span class="sxs-lookup"><span data-stu-id="d05bd-157">If it is not null, an operation in the method has failed, and `tempPort` is closed to make sure that any resources are released.</span></span> <span data-ttu-id="d05bd-158">如果方法的操作成功，则返回的端口对象将包含打开的 SerialPort 对象，如果操作失败，则值为 null。</span><span class="sxs-lookup"><span data-stu-id="d05bd-158">The returned port object will contain the opened SerialPort object if the operations of the method succeeded, or it will be null if an operation failed.</span></span>

```csharp
public SerialPort OpenPort1(string portName)
{
   SerialPort port = new SerialPort(portName);
   port.Open();  //CA2000 fires because this might throw
   SomeMethod(); //Other method operations can fail
   return port;
}

public SerialPort OpenPort2(string portName)
{
   SerialPort tempPort = null;
   SerialPort port = null;
   try
   {
      tempPort = new SerialPort(portName);
      tempPort.Open();
      SomeMethod();
      //Add any other methods above this line
      port = tempPort;
      tempPort = null;

   }
   finally
   {
      if (tempPort != null)
      {
         tempPort.Close();
      }
   }
   return port;
}
```

```vb
Public Function OpenPort1(ByVal PortName As String) As SerialPort

   Dim port As New SerialPort(PortName)
   port.Open()    'CA2000 fires because this might throw
   SomeMethod()   'Other method operations can fail
   Return port

End Function

Public Function OpenPort2(ByVal PortName As String) As SerialPort

   Dim tempPort As SerialPort = Nothing
   Dim port As SerialPort = Nothing

   Try
      tempPort = New SerialPort(PortName)
      tempPort.Open()
      SomeMethod()
      'Add any other methods above this line
      port = tempPort
      tempPort = Nothing

   Finally
      If Not tempPort Is Nothing Then
         tempPort.Close()
      End If

   End Try

   Return port

End Function
```

## <a name="example-2"></a><span data-ttu-id="d05bd-159">示例 2</span><span class="sxs-lookup"><span data-stu-id="d05bd-159">Example 2</span></span>

<span data-ttu-id="d05bd-160">默认情况下，Visual Basic 编译器会检查溢出情况下的所有算术运算符。</span><span class="sxs-lookup"><span data-stu-id="d05bd-160">By default, the Visual Basic compiler has all arithmetic operators check for overflow.</span></span> <span data-ttu-id="d05bd-161">因此，任何 Visual Basic 算术运算都可能引发 <xref:System.OverflowException>。</span><span class="sxs-lookup"><span data-stu-id="d05bd-161">Therefore, any Visual Basic arithmetic operation might throw an <xref:System.OverflowException>.</span></span> <span data-ttu-id="d05bd-162">这可能会导致 CA2000 等规则出现意外冲突。</span><span class="sxs-lookup"><span data-stu-id="d05bd-162">This could lead to unexpected violations in rules such as CA2000.</span></span> <span data-ttu-id="d05bd-163">例如，以下 CreateReader1 函数会产生 CA2000 冲突，因为 Visual Basic 编译器正在为加法发出的溢出检查指令可能会引发导致 StreamReader 无法释放的异常。</span><span class="sxs-lookup"><span data-stu-id="d05bd-163">For example, the following CreateReader1 function will produce a CA2000 violation because the Visual Basic compiler is emitting an overflow checking instruction for the addition that could throw an exception that would cause the StreamReader not to be disposed.</span></span>

<span data-ttu-id="d05bd-164">要解决此问题，可在项目中禁用 Visual Basic 编译器发出溢出检查，也可修改代码，如以下 CreateReader2 函数所示。</span><span class="sxs-lookup"><span data-stu-id="d05bd-164">To fix this, you can disable the emitting of overflow checks by the Visual Basic compiler in your project or you can modify your code as in the following CreateReader2 function.</span></span>

<span data-ttu-id="d05bd-165">若要禁用发出溢出检查，请在解决方案资源管理器中右键单击项目名称，然后单击“属性”。</span><span class="sxs-lookup"><span data-stu-id="d05bd-165">To disable the emitting of overflow checks, right-click the project name in Solution Explorer and then click **Properties**.</span></span> <span data-ttu-id="d05bd-166">依次单击“编译”和“高级编译选项”，然后检查“不做整数溢出检查”  。</span><span class="sxs-lookup"><span data-stu-id="d05bd-166">Click **Compile**, click **Advanced Compile Options**, and then check **Remove integer overflow checks**.</span></span>

:::code language="vb" source="snippets/vb/all-rules/ca2000-dispose-objects-before-losing-scope-vboverflow_1.vb":::

## <a name="see-also"></a><span data-ttu-id="d05bd-167">另请参阅</span><span class="sxs-lookup"><span data-stu-id="d05bd-167">See also</span></span>

- <xref:System.IDisposable>
- [<span data-ttu-id="d05bd-168">释放模式</span><span class="sxs-lookup"><span data-stu-id="d05bd-168">Dispose Pattern</span></span>](../../../standard/garbage-collection/implementing-dispose.md)
